apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: dev-applicationset
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
    - matrix:
        generators:
          - git:
              repoURL: https://github.com/mdouek/argo-appset.git
              revision: HEAD
              files:
                - path: "dev-cluster-app-mapping.yaml"
          - list:
              elements: []
              elementsYaml: "{{ .clusters | toJson }}"
  template:
    metadata:
      name: 'test'
    spec:
      project: default
      source:
        repoURL: |
          {{- if .applications.0.localChart }}
          https://github.com/mdouek/argo-appset.git
          {{- else }}
          {{ .applications.0.repoURL }}
          {{- end }}
        targetRevision: |
          {{- if .applications.0.localChart }}
          HEAD
          {{- else }}
          {{ .applications.0.version }}
          {{- end }}
        chart: |
          {{- if .applications.0.localChart }}
          null
          {{- else }}
          {{ .applications.0.chart }}
          {{- end }}
        path: |
          {{- if .applications.0.localChart }}
          charts/{{ .applications.0.chart }}
          {{- else }}
          null
          {{- end }}
        helm:
          valueFiles:
            - 'envs/dev/environment-values.yaml'
            - 'app-values/{{ .applications.0.name }}-values.yaml'
            - '{{ .applications.0.valuesFile }}'
      destination:
        server: 'https://kubernetes.{{ .cluster }}.svc'
        namespace: '{{ .applications.0.namespace }}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
  syncPolicy:
    preserveResourcesOnDeletion: true