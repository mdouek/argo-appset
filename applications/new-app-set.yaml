apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: dev-applicationset
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - matrix:
        generators:
          - clusters: 
              selector:
                matchLabels:
                  io.portainer.kubernetes.application.name: in-cluster2
          - git:
              repoURL: https://github.com/mdouek/argo-appset.git
              revision: HEAD
              files:
                - path: "mappings/dev/*/applications.yaml"
  template:
    metadata:
      name: 'dev-{{ .clusterName }}-{{ .name }}'
    spec:
      project: default
      destination:
        name: '{{ .clusterName }}'
        namespace: '{{ .namespace }}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
  templatePatch: |
    spec:
      sources:
      - repoURL: '{{ .repoURL | default "https://github.com/mdouek/argo-appset.git" }}'
        targetRevision: '{{ .targetRevision | default "HEAD" }}'
        {{- if .chart }}
        chart: '{{ .chart }}'
        path: null
        {{- else }}
        path: '{{ .chartPath }}'
        chart: null
        {{- end }}
        helm:
          ignoreMissingValueFiles: true
          valueFiles:
            - '$values/envs/dev/environment-values.yaml'
            - '$values/app-values/{{ .name }}-values.yaml'
            - '$values/envs/dev/{{ .name }}-values.yaml'
      - repoURL: 'https://github.com/mdouek/argo-appset.git'
        targetRevision: HEAD
        ref: values
  syncPolicy:
    preserveResourcesOnDeletion: true
